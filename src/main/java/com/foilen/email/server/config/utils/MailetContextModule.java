package com.foilen.email.server.config.utils;

// This module is no longer needed - MailetContainerModule provides these bindings
// Keeping the file for reference but it's empty
import com.google.inject.AbstractModule;

public class MailetContextModule extends AbstractModule {

    @Override
    protected void configure() {
        // Empty - not used anymore
    }
}
